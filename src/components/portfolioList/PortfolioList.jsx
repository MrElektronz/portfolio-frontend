import "./portfolioList.scss";
import React from "react";

/*
export default function PortfolioList({title, active, setSelected, id, i}) {
  return <li key={i} className={active ? "portfolioList active" : "portfolioList"}
  onClick={()=>setSelected(id)}>
      {title}
  </li>;
}
*/